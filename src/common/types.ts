/**
 * In the current file, declare type
 */
export type Env = 'dev' | 'test' | 'pre' | 'prod';

/**
 * Only declared variables can be used
 */
export type StoreKeys = 'database'| 'configs' | 'logger' | 'redis';
